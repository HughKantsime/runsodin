apiVersion: apps/v1
kind: Deployment
metadata:
  name: odin
  namespace: odin-demo
  labels:
    app: odin
    odin_env: demo
  annotations:
    odin/issue: ODIN-128
spec:
  replicas: 1
  strategy:
    type: Recreate
  selector:
    matchLabels:
      app: odin
  template:
    metadata:
      labels:
        app: odin
        odin_env: demo
    spec:
      containers:
      - name: odin
        image: ghcr.io/hughkantsime/odin:latest
        imagePullPolicy: Always
        ports:
        - containerPort: 8000
          name: http
        env:
        - name: ENCRYPTION_KEY
          valueFrom:
            secretKeyRef:
              name: odin-demo-secrets
              key: encryption_key
        - name: JWT_SECRET_KEY
          valueFrom:
            secretKeyRef:
              name: odin-demo-secrets
              key: jwt_secret_key
        - name: API_KEY
          valueFrom:
            secretKeyRef:
              name: odin-demo-secrets
              key: api_key
        - name: DATABASE_URL
          value: sqlite:////data/odin.db
        - name: CORS_ORIGINS
          value: https://demo.subsystem.app
        - name: ODIN_BAMBU_BROKER_URL
          value: mqtt://mosquitto:1883
        # ODIN-142 Wake 2b: Bambu broker policy resolver allowlist. Both
        # gates required for ODIN to subscribe plain TCP to in-cluster
        # mosquitto. Production deploys leave these unset → TLS:8883.
        - name: ODIN_ALLOW_INSECURE_BAMBU_BROKER
          value: "1"
        - name: ODIN_BAMBU_INSECURE_BROKER_HOSTS
          value: "mosquitto,localhost,127.0.0.1"
        - name: ODIN_BAMBU_INSECURE_BROKER_PORT
          value: "1883"
        - name: ODIN_PUBLIC_URL
          value: https://demo.subsystem.app
        - name: ODIN_DEMO_MODE
          value: "1"
        - name: ODIN_DEMO_EDU_SEED
          value: "1"
        - name: ODIN_LICENSE_READ_ONLY
          value: "1"
        - name: ODIN_DEMO_EDU_ADMIN_EMAIL
          valueFrom:
            secretKeyRef: {name: demo-reviewer-credentials, key: edu_admin_email}
        - name: ODIN_DEMO_EDU_ADMIN_PASSWORD
          valueFrom:
            secretKeyRef: {name: demo-reviewer-credentials, key: edu_admin_password}
        - name: ODIN_DEMO_EDU_TEACHER_EMAIL
          valueFrom:
            secretKeyRef: {name: demo-reviewer-credentials, key: edu_teacher_email}
        - name: ODIN_DEMO_EDU_TEACHER_PASSWORD
          valueFrom:
            secretKeyRef: {name: demo-reviewer-credentials, key: edu_teacher_password}
        - name: ODIN_DEMO_EDU_STUDENT_EMAIL
          valueFrom:
            secretKeyRef: {name: demo-reviewer-credentials, key: edu_student_email}
        - name: ODIN_DEMO_EDU_STUDENT_PASSWORD
          valueFrom:
            secretKeyRef: {name: demo-reviewer-credentials, key: edu_student_password}
        - name: ODIN_DEMO_REVIEWER_EMAIL
          valueFrom:
            secretKeyRef: {name: demo-reviewer-credentials, key: email}
        - name: ODIN_DEMO_REVIEWER_PASSWORD
          valueFrom:
            secretKeyRef: {name: demo-reviewer-credentials, key: password}
        - name: TZ
          value: UTC
        readinessProbe:
          httpGet:
            path: /health
            port: 8000
          initialDelaySeconds: 10
          periodSeconds: 10
          failureThreshold: 6
        livenessProbe:
          httpGet:
            path: /health
            port: 8000
          initialDelaySeconds: 60
          periodSeconds: 30
          failureThreshold: 5
        volumeMounts:
        - name: data
          mountPath: /data
        - name: license
          mountPath: /data/odin.license
          subPath: odin.license
          readOnly: true
        resources:
          limits:
            cpu: "1"
            memory: 1Gi
          requests:
            cpu: 250m
            memory: 384Mi
      volumes:
      - name: data
        persistentVolumeClaim:
          claimName: odin-demo-data
      - name: license
        secret:
          secretName: odin-demo-license
---
apiVersion: v1
kind: Service
metadata:
  name: odin
  namespace: odin-demo
  labels:
    app: odin
spec:
  type: ClusterIP
  selector:
    app: odin
  ports:
  - name: http
    port: 8000
    targetPort: 8000

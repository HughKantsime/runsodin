# O.D.I.N. — Environment Configuration
# Copy to .env: cp .env.example .env

# Leave blank to auto-generate on first run (recommended)
ENCRYPTION_KEY=
JWT_SECRET_KEY=
API_KEY=
TRUSTED_HOSTS=localhost,127.0.0.1
TZ=America/New_York
